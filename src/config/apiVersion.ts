/**
 * Versão semântica exposta na API (contrato HTTP/JSON).
 * Alinhar com releases; documentar breaking changes no README.
 */
export const API_SEMANTIC_VERSION = "1.0.0" as const;

/** Identificador curto do prefixo de rota `/api/v1`. */
export const API_ROUTE_VERSION = "v1" as const;
